% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/net_benefit.R
\name{simulate_supplemental_net_benefit}
\alias{simulate_supplemental_net_benefit}
\title{Simulate a supplemental (area) SCO/ECO endorsement's per-draw net benefit}
\usage{
simulate_supplemental_net_benefit(
  data,
  area_loss_start_percent,
  area_loss_end_percent,
  base_rate,
  rate_differential = NULL,
  subsidy_percent,
  net_benefit_label,
  indemnity_label = NULL
)
}
\arguments{
\item{data}{data.table exploded to one row per agent x draw. Must contain
\code{underlying_liability_amount}, \code{coverage_level_percent},
\code{expected_county_yield}, \code{final_county_yield}, \code{harvest_price},
\code{projected_price}, \code{insurance_plan_code}, and the band/rate/subsidy columns
named by the string arguments below.}

\item{area_loss_start_percent, area_loss_end_percent}{column names of the band
top and bottom (coverage ratios).}

\item{base_rate}{column name of the supplemental base premium rate.}

\item{rate_differential}{optional column name of a multiplicative premium-rate
differential (OBBBA recalibrations); \code{NULL} uses \code{base_rate} alone.}

\item{subsidy_percent}{column name of the supplemental subsidy share.}

\item{net_benefit_label}{name of the net-benefit column to create
(\code{indemnity - premium + subsidy}).}

\item{indemnity_label}{optional name under which to keep a copy of the per-draw
supplemental indemnity. \code{NULL} (default) drops it with the other
intermediates.}
}
\value{
\code{data} (modified in place); the intermediate supplemental columns are
dropped, leaving \code{net_benefit_label} (and \code{indemnity_label} if requested).
}
\description{
Adds an area-triggered, loss-contingent supplemental net benefit to a
draw-level agent table. Supplemental liability is the underlying liability
scaled to the endorsement band (\code{area_loss_start_percent - area_loss_end_percent});
premium and subsidy are actuarial. The indemnity is the M-13 payment rate on
the COUNTY (pool) draw (\code{final_county_yield} vs \code{expected_county_yield}), so
payouts follow the area outcome and basis risk is preserved. \code{trigger_index}
is the band top (coverage ratio) and \code{coverage_range} the band width; the
underlying \code{insurance_plan_code} selects yield vs revenue-to-count logic.
}
