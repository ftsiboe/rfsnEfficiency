% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario_pipeline.R
\name{assemble_scenario_outcomes}
\alias{assemble_scenario_outcomes}
\title{Assemble scenario outcome columns from a baseline plus a component recipe}
\usage{
assemble_scenario_outcomes(data, baseline, recipe)
}
\arguments{
\item{data}{data.table containing \code{baseline} and every \code{component}/\code{scale}
column referenced by \code{recipe}.}

\item{baseline}{name of the baseline outcome column added to every scenario.}

\item{recipe}{data.table with columns \code{scenario} (output column name),
\code{component} (column to add; \code{NA} = baseline only), and \code{scale} (optional
column to multiply the component by; \code{NA} = unscaled).}
}
\value{
\code{data} (modified in place) with one new column per unique \code{scenario}.
}
\description{
Builds one outcome column per scenario as \code{baseline + sum(scale * component)},
driven by a long recipe table. Each recipe row adds one component column
(optionally scaled by another column, e.g. an adoption rate) onto the named
scenario; scenarios with no component rows (or a single \code{NA} component) equal
the baseline. Analysis-agnostic: the scenario design lives entirely in \code{recipe}.
}
