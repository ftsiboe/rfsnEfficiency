% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario_pipeline.R
\name{balance_scenarios}
\alias{balance_scenarios}
\title{Balance a scored panel across scenario cells by pruning incomplete units}
\usage{
balance_scenarios(
  dt,
  unit,
  cell,
  require_finite = NULL,
  allow_missing = NULL,
  prun_to_rebalance = TRUE,
  min_retention = 0.5,
  weight = NULL,
  verbose = TRUE
)
}
\arguments{
\item{dt}{data.table with one row per \code{unit} x \code{cell}, plus the metric columns.}

\item{unit}{character vector of columns identifying a unit (the thing that
must be common across cells; carried whole when kept).}

\item{cell}{character vector of columns defining a scenario cell.}

\item{require_finite}{optional character vector of metric columns that must be
finite for a row to count as covering its cell. \code{NULL} (default) requires
only row presence.}

\item{allow_missing}{optional named list mapping \code{cell} columns to value(s);
cells whose columns all match are dropped from the required grid (a unit is
not penalised for missing them).}

\item{prun_to_rebalance}{if \code{TRUE} (default) drop units that do not cover the
full required grid and return the rebalanced panel; if \code{FALSE} return \code{dt}
unchanged (report-only / dry run).}

\item{min_retention}{when pruning, \code{stop()} if the retained share (weighted by
\code{weight} if supplied, else by unit count) falls below this fraction --
heavy pruning signals a structural problem, not routine trimming.}

\item{weight}{optional per-unit weight column used for the retention share and
the report.}

\item{verbose}{if \code{TRUE} (default) message a one-line retention summary.}
}
\value{
a data.table: the rebalanced panel (or \code{dt} unchanged when
\code{prun_to_rebalance = FALSE}), with a \code{"balance_report"} attribute (per-cell
valid-unit counts before, uniform count after, and overall retention).
}
\description{
Guards cross-scenario comparisons against a hidden composition confound: a
\code{unit} (e.g. a producer) should contribute to \emph{every} scenario \code{cell} (e.g.
every policy environment x program combination x participation), otherwise
differences across cells can mix treatment with sample composition. This
checks whether each unit is present -- and, if \code{require_finite} is given, has
finite values for those metric columns -- in every required cell, and (by
default) prunes to the units that are, returning a rebalanced panel.
}
\details{
The required grid is the set of observed \code{cell} combinations minus any listed
in \code{allow_missing} (for structurally absent combinations, e.g. a base arm that
has no adoption pathway). Pruning at the finest unit level means every coarser
aggregation (subgroup) inherits the balance automatically.
}
