% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario_pipeline.R
\name{score_scenarios}
\alias{score_scenarios}
\title{Score many outcome columns against a common baseline}
\usage{
score_scenarios(
  data,
  outcome_cols,
  by,
  baseline_col = "revenue00",
  scenario_map = NULL,
  weight = NULL,
  scenario_col = "scenario_col"
)
}
\arguments{
\item{data}{data.table with \code{by}, \code{baseline_col}, and every \code{outcome_cols}
column; one row per group x draw.}

\item{outcome_cols}{character vector of treated outcome columns to score.}

\item{by}{character vector of grouping columns (the scored unit; draws are the
rows within each group).}

\item{baseline_col}{name of the no-treatment baseline column.}

\item{scenario_map}{optional data.table keyed by \code{scenario_col} to merge
scenario labels onto the scores.}

\item{weight}{optional name of a per-group weight column in \code{data} to carry
onto the scores.}

\item{scenario_col}{name of the column identifying the scored outcome column
(default \code{"scenario_col"}).}
}
\value{
a data.table of scores, one row per \code{by} group x scenario.
}
\description{
Loops over \code{outcome_cols}, scoring each against \code{baseline_col} with
\code{compute_efficiency_scores()} (draws = the rows within each \code{by} group), and
row-binds the per-scenario scores. Optionally merges a \code{scenario_map} of labels
and attaches a per-group \code{weight}. Analysis-agnostic wrapper.
}
