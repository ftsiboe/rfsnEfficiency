% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_efficiency_analysis.R
\name{run_efficiency_analysis}
\alias{run_efficiency_analysis}
\title{Run an on-demand efficiency analysis}
\usage{
run_efficiency_analysis(
  data,
  by,
  regime_col = "regime",
  outcome_args = NULL,
  ref = NULL,
  alt = NULL
)
}
\arguments{
\item{data}{data.frame/data.table of simulated outcomes. Must contain the
\code{by} columns and either (\code{outcome_baseline}, \code{outcome_treated}) or the raw
components that \code{build_outcome()} needs (then pass \code{outcome_args}).}

\item{by}{character vector of grouping columns (e.g.,
\code{c("group_id","scenario")}).}

\item{regime_col}{regime column name (default "regime"); created as "all" if
absent.}

\item{outcome_args}{named list of arguments forwarded to \code{build_outcome()}
when the outcome columns are not already present (e.g.,
\code{list(base_value = "outcome", transfers = "indemnity", premium = "premium")}).}

\item{ref, alt}{reference and alternative regime labels for the deltas. If
\code{NULL} (default) and exactly/at least two regimes are present, the first two
(sorted) are used.}
}
\value{
list with \code{scores} (per regime) and \code{deltas} (alt - ref).
}
\description{
High-level driver: given simulated outcomes (one row per draw/year x group,
optionally tagged by regime/scenario), build the outcome if needed, compute
the efficacy/efficiency scores per regime, and difference regimes.
}
\examples{
# Self-contained: synthetic outcomes, no external data required.
d <- simulate_example_outcomes()
res <- run_efficiency_analysis(
  data = d, by = c("group_id", "scenario"),
  outcome_args = list(base_value = "outcome", transfers = "indemnity",
                      premium = "premium"))
head(res$scores)
head(res$deltas)
}
