% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario_pipeline.R
\name{explode_draws}
\alias{explode_draws}
\title{Explode nested draw list-columns to long form}
\usage{
explode_draws(
  data,
  draw_columns,
  value_names = draw_columns,
  drop = character(0)
)
}
\arguments{
\item{data}{data.frame/data.table with the list-columns named in
\code{draw_columns}; all list-columns must have the same length within a row.}

\item{draw_columns}{character vector of list-column names to unnest.}

\item{value_names}{names for the unnested output columns (defaults to
\code{draw_columns}).}

\item{drop}{character vector of columns to discard (e.g. draw metadata not
needed downstream).}
}
\value{
a data.table, long over draws.
}
\description{
Unnests one or more equal-length list-columns (e.g. per-agent Monte-Carlo draw
vectors) into a long table with one row per input row x draw element. Every
column that is neither a draw column nor dropped is carried (recycled) onto the
expanded rows. Analysis-agnostic.
}
