% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/net_benefit.R
\name{simulate_basic_policy_net_benefit}
\alias{simulate_basic_policy_net_benefit}
\title{Simulate a basic (individual) policy's per-draw net benefit}
\usage{
simulate_basic_policy_net_benefit(
  data,
  subsidy_amount,
  net_benefit_label,
  indemnity_label = NULL
)
}
\arguments{
\item{data}{data.table exploded to one row per agent x draw. Must contain
\code{underlying_liability_amount}, \code{underlying_total_premium_amount},
\code{approved_yield}, \code{yield_farm}, \code{harvest_price}, \code{projected_price},
\code{coverage_level_percent}, \code{insurance_plan_code}, and the \code{subsidy_amount}
column named below.}

\item{subsidy_amount}{name of the per-draw subsidy-amount column to add in.}

\item{net_benefit_label}{name of the net-benefit column to create
(\code{indemnity - premium + subsidy}).}

\item{indemnity_label}{optional name for a copy of the per-draw indemnity to
keep on \code{data}. \code{NULL} (default) leaves only \code{underlying_indemnity_amount}.}
}
\value{
\code{data} (modified in place); invisibly, with
\code{underlying_indemnity_amount}, \code{net_benefit_label}, and (if requested)
\code{indemnity_label} added.
}
\description{
Adds an individual, loss-contingent indemnity and a scenario net benefit to a
draw-level agent table. The indemnity is the M-13 payment rate applied to the
simulated farm yield draw (\code{yield_farm}) against the guarantee, times the
underlying liability; it is triggered at the coverage level (full payout at
total loss). Because the indemnity does not depend on the subsidy scenario it
is computed once (guarded on \code{underlying_indemnity_amount}) and reused.
}
