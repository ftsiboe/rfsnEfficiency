% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outcome.R
\name{build_outcome}
\alias{build_outcome}
\title{Construct the analysis outcome and its no-safety-net baseline}
\usage{
build_outcome(
  data,
  base_value = "revenue",
  cost = NULL,
  transfers = "indemnity",
  cost_protection = NULL,
  premium = "producer_premium",
  floor_at = 0
)
}
\arguments{
\item{data}{data.frame/data.table with the component columns.}

\item{base_value}{column name of the gross pre-safety-net outcome
(e.g., "revenue"; for profit pass revenue plus a \code{cost}).}

\item{cost}{optional column subtracted from \code{base_value}. May be a stochastic
per-observation series so cost risk enters the outcome (enabling cost-side
products). \code{NULL} = revenue; a production-cost column = profit.}

\item{transfers}{character vector of revenue/yield-side payout columns ADDED
to the treated outcome (e.g., \code{c("indemnity","title1_payment")}). \code{NULL}
for products that provide no revenue-side protection.}

\item{cost_protection}{character vector of cost-side payout columns (a product
that pays when costs rise) ADDED to the treated outcome. \code{NULL} if none.}

\item{premium}{column SUBTRACTED from the treated outcome (producer-paid
premium for whichever product(s) are active); \code{NULL} to ignore.}

\item{floor_at}{numeric floor applied to both series (default 0; \code{NA} = none).}
}
\value{
\code{data} with new columns \code{outcome_baseline} and \code{outcome_treated}.
}
\description{
Construct the analysis outcome and its no-safety-net baseline
}
\examples{
\dontrun{
build_outcome(d, base_value = "revenue", cost = NULL)                 # revenue
build_outcome(d, base_value = "revenue", cost = "input_cost")         # profit
build_outcome(d, "revenue", cost = "input_cost", transfers = NULL,    # cost-only
              cost_protection = "margin_indemnity", premium = "margin_premium")
}
}
