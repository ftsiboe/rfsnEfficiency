% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_insurance_payment_rate.R
\name{calc_insurance_payment_rate}
\alias{calc_insurance_payment_rate}
\title{Calculate an insurance payment rate (indemnification)}
\usage{
calc_insurance_payment_rate(
  expected_yield,
  final_yield,
  harvest_price,
  price_election_amount,
  projected_price,
  trigger_index,
  coverage_range = 1,
  insurance_plan_code
)
}
\arguments{
\item{expected_yield}{Numeric. Expected yield level (e.g. APH / approved yield).}

\item{final_yield}{Numeric. Realized harvested yield.}

\item{harvest_price}{Numeric. Market price at harvest (used for
revenue-to-count in some plans).}

\item{price_election_amount}{Numeric. Elected price at sign-up (typically
\code{projected_price * price_election_percent}).}

\item{projected_price}{Numeric. Projected / initial price used for guarantee
construction and caps.}

\item{trigger_index}{Numeric. Coverage trigger as a proportion (e.g. the
coverage level in \verb{[0, 1]}).}

\item{coverage_range}{Numeric. Coverage range width; default \code{1}.}

\item{insurance_plan_code}{Character or numeric. Plan code that determines
whether the guarantee may use harvest price and whether revenue-to-count
uses the harvest or the elected price.}
}
\value{
A named list with \code{new_price_election_amount},
\code{new_insurance_guarantee}, \code{revenue_to_count}, and \code{payment_rate}.
}
\description{
Computes a payment rate in \verb{[0, 1]} from expected and realized yields plus
price inputs, and returns the intermediate guarantee / revenue-to-count
quantities. Vectorized over its inputs.
}
\details{
Plan-code logic is implementation-specific: \code{insurance_plan_code} membership
tests decide (a) whether harvest price can raise the guarantee, and (b)
whether revenue-to-count uses \code{harvest_price} or \code{price_election_amount}.
Ensure your plan-code conventions match those encoded here.
}
\seealso{
\code{\link[=simulate_basic_policy_net_benefit]{simulate_basic_policy_net_benefit()}},
\code{\link[=simulate_supplemental_net_benefit]{simulate_supplemental_net_benefit()}}
}
