% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example_data.R
\name{simulate_example_outcomes}
\alias{simulate_example_outcomes}
\title{Simulate a tiny example outcomes dataset (no external data required)}
\usage{
simulate_example_outcomes(
  n_groups = 6,
  n_draws = 200,
  regimes = c("baseline", "reform"),
  seed = 1
)
}
\arguments{
\item{n_groups}{number of groups (e.g., units / commodities / portfolios).}

\item{n_draws}{draws (years / Monte Carlo) per group per scenario per regime.}

\item{regimes}{character vector of regime labels.}

\item{seed}{RNG seed for reproducibility.}
}
\value{
a \code{data.table} with \code{group_id}, \code{scenario}, \code{regime}, \code{outcome},
\code{indemnity}, and \code{premium}.
}
\description{
Generates synthetic per-draw outcomes for two regimes and a few scenarios so
the package can be demonstrated, tested, and learned standalone. This is an
ILLUSTRATIVE generator, not a calibrated model -- the numbers carry no meaning.
}
\examples{
d <- simulate_example_outcomes(n_groups = 3, n_draws = 50)
head(d)
}
