% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent_outcomes.R
\name{build_agent_outcomes}
\alias{build_agent_outcomes}
\title{Build per-agent baseline/treated outcomes from end-of-season draws + parameters}
\usage{
build_agent_outcomes(
  agents,
  indemnity,
  outcome = "outcome",
  cost = 0,
  premium = 0,
  subsidy = 0,
  agent_id_col = "agent_id",
  extra_cols = NULL,
  floor_at = 0
)
}
\arguments{
\item{agents}{a list with one element per agent. Each element is either a
numeric vector of the base end-of-season outcome (draws), or a
data.frame/data.table of draws (rows = crop years / Monte Carlo draws)
containing at least the \code{outcome} column. List names, if present, become
agent ids.}

\item{indemnity}{indemnification rule: a \verb{function(agent_df)} returning a
per-draw indemnity vector, OR a column name in each agent's data, OR a
numeric scalar/vector. Indemnity is ADDED to the treated outcome.}

\item{outcome}{name of the base end-of-season outcome column (default
"outcome"); ignored when an agent element is a plain numeric vector.}

\item{cost, premium, subsidy}{program parameters, each a \verb{function(agent_df)},
a column name, or a numeric scalar/vector. \code{cost} is subtracted from the
base outcome (enters both baseline and treated). \code{premium} is the
producer-paid premium, subtracted from the treated outcome. \code{subsidy} is
carried through for program-context summaries (not added to the outcome).}

\item{agent_id_col}{name of the agent id column in the output (default "agent_id").}

\item{extra_cols}{optional character vector of additional per-draw columns to
carry through from each agent's data (e.g., "commodity_code", "combination",
"regime", "commodity_year").}

\item{floor_at}{numeric floor applied to both outcome series (default 0;
\code{NA} = none).}
}
\value{
a \code{data.table} stacked over agents with \code{agent_id}, \code{draw},
\code{outcome_baseline}, \code{outcome_treated}, and the resolved \code{cost},
\code{producer_premium}, \code{subsidy}, \code{indemnity} columns (plus \code{extra_cols}).
}
\description{
The flexible entry point: hand in each agent's underlying end-of-season
outcomes and the program parameters, and get back stacked baseline/treated
outcome draws ready for \code{compute_efficiency_scores()}.
}
\details{
\deqn{outcome\_baseline = outcome - cost}
\deqn{outcome\_treated  = outcome\_baseline + indemnity - producer\_premium}
}
\examples{
# Two agents, each with 100 end-of-season revenue draws; indemnity is a
# function paying 70\% of the shortfall below a 600 guarantee.
set.seed(1)
agents <- list(
  cornA = data.frame(outcome = pmax(0, rnorm(100, 600, 150))),
  cornB = data.frame(outcome = pmax(0, rnorm(100, 550, 200)))
)
indem <- function(df) pmax(0, 600 - df$outcome) * 0.70
d <- build_agent_outcomes(agents, indemnity = indem, premium = 25, subsidy = 0.62)
compute_efficiency_scores(d, by = "agent_id")[, .(agent_id, mean_index, risk_index, efficiency)]
}
